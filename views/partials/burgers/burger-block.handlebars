<li>
	{{name}}

	<button class=".change-state" data-id="{{id}}" data-newDevoured="{{eaten}}">
		{{!-- {{#if eaten}}SLEEP TIME!{{else}}WAKE UP!{{/if}} --}}
        {{#if eaten}}YUM YUM!{{/if}}
	</button>

	<button class="delete" data-id="{{this.id}}">DELETE!</button>
</li>
